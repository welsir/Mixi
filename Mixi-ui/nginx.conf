server {
    listen 3000;
    server_name localhost;
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
    location /chat {
        proxy_pass http://123.249.107.238:8090;
        proxy_http_version 1.1;
    }
}
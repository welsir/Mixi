server:
  port: ${SERVER_PORT:9020}

spring:
  application:
    name: Mixi-webRoom
  config:
    import: classpath:you-cant-see-that.yml
  profiles:
    active: ${ACTIVE:dev}

  cloud:
    api: true

#logging:
#  level:
#    org.springframework: DEBUG
